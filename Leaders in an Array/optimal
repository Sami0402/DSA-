def findLeaders(a):
    result = []
    n = len(a)
    maxi = float('-inf')
    for i in range(n-1 , -1, -1):
        if a[i] > maxi:
            result.append(a[i])
            maxi = a[i]
    
    result.reverse()
        
    return result
    

a = [10, 22, 12, 3, 0, 6]

print(findLeaders(a))
