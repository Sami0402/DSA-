mini = prices[0]
        profit = 0

        for i in range(1,len(prices)):
            cost = prices[i] - mini

            profit = max(cost, profit)

            mini = min(prices[i], mini)

        if (profit >= 1):
            return profit
        else:
            return 0
