def majElem(a):
    n = len(a) // 2
    
    for i in range(len(a)):
        cnt = 0
        for j in range(len(a)):
            if a[i] == a[j]:
                cnt += 1
        
        if cnt > n:
            return a[i]
    return -1

a = [2,3,3,3,3,2,2]

print(majElem(a))
